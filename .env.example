# Copia este archivo a .env y rellena tus credenciales

# Blotato API Key (obligatorio)
# Obtener en: https://my.blotato.com/settings/api
BLOTATO_API_KEY=your_blotato_api_key_here

# LLM para escribir los posts (al menos uno requerido)
# Anthropic Claude — https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Perplexity (alternativa, usa sonar-pro) — https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# Transcripción de audio (requerida SOLO para la fuente "Audio" — notas de voz)
# Motor: "api" (Whisper en la nube, default) o "local" (faster-whisper, gratis).
# TRANSCRIPTION_ENGINE=api
#
# ── Opción gratuita: faster-whisper en local (sin clave, offline) ──────────────
# Pon TRANSCRIPTION_ENGINE=local e instala: pip install faster-whisper
# El modelo se descarga una vez en el primer uso. No hay límite de 25 MB.
# TRANSCRIPTION_ENGINE=local
# TRANSCRIPTION_LOCAL_MODEL=base       # tiny | base | small | medium | large-v3 (mayor = mejor y más lento)
# TRANSCRIPTION_LOCAL_DEVICE=cpu       # cpu | cuda (si tienes GPU NVIDIA)
# TRANSCRIPTION_LOCAL_COMPUTE=int8     # int8 (CPU) | float16 (GPU)
#
# ── Opción API: endpoint Whisper compatible con OpenAI ─────────────────────────
# Con OPENAI_API_KEY basta (default). OpenAI — https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Alternativa: Groq (también compatible con OpenAI). Si lo usas, define la clave
# en GROQ_API_KEY y apunta la base URL y el modelo:
# GROQ_API_KEY=
# TRANSCRIPTION_BASE_URL=https://api.groq.com/openai/v1
# TRANSCRIPTION_MODEL=whisper-large-v3
# (Overrides opcionales para OpenAI — default whisper-1)
# TRANSCRIPTION_BASE_URL=https://api.openai.com/v1
# TRANSCRIPTION_MODEL=whisper-1

# IDs de cuentas para publicar (opcional)
# Si se dejan vacíos, la API los lista automáticamente al primer job.
BLOTATO_LINKEDIN_ACCOUNT_ID=
BLOTATO_INSTAGRAM_ACCOUNT_ID=
BLOTATO_FACEBOOK_ACCOUNT_ID=
# TikTok solo se usa como destino de reels (video vertical).
BLOTATO_TIKTOK_ACCOUNT_ID=

# ── Generación de imágenes y VIDEO — Higgsfield MCP (backend ACTIVO) ───────────
# Imágenes y video se generan vía el MCP oficial de Higgsfield
# (https://mcp.higgsfield.ai), que autentica por OAuth contra TU CUENTA: consume
# los créditos de tu SUSCRIPCIÓN (no el Cloud API, que tiene un pool aparte).
#
# Setup: desde la página /conexiones de la web (botón "Conectar" → consentimiento
# en el navegador), o por terminal:
#     cd api/scripts && python mcp_bootstrap.py
# Ambos dejan el token store en api/.hf_oauth.json (NO lo commitees). Con ese
# archivo presente, la web usa el MCP sola. Si falta, se usan las
# PLANTILLAS LOCALES de respaldo (api/assets/templates/template-1..3.png,
# 1080x1080) con el copy superpuesto. El VIDEO no tiene fallback (son imágenes).
#
# Overrides opcionales (defaults sensatos):
# HIGGSFIELD_MCP_TOKEN_STORE=                 # ruta del token store (default api/.hf_oauth.json)
# HIGGSFIELD_MCP_IMAGE_MODEL=nano_banana_pro  # default de imagen si el form/sheet no elige modelo.
#                                             # Catálogo por post (créditos/imagen, jul 2026):
#                                             # nano_banana_pro 2 | nano_banana_2 1.5 | nano_banana 1
#                                             # | gpt_image_2 0.5 | z_image 0.15 (ver --models image)
# HIGGSFIELD_MCP_VIDEO_MODEL=kling3_0_turbo   # default de video si el form/sheet no elige modelo.
#                                             # Catálogo por post (créditos/segundo, jul 2026):
#                                             # kling3_0_turbo 1.5 | seedance_2_0_mini 2.5 | seedance_2_0 4.5
# HIGGSFIELD_VIDEO_ASPECT=9:16                # aspect del clip de feed (16:9|4:3|1:1|9:16)
# HIGGSFIELD_VIDEO_SEGMENT_SECONDS=5          # segundos por SEGMENTO generado. Un reel largo
#                                             # (p. ej. 30s) se arma concatenando varios segmentos
#                                             # (Kling produce ~5-10s por generación). La duración
#                                             # total buscada se elige por post (form/sheet: duracion_video).
# HIGGSFIELD_VIDEO_DURATION=                  # (legacy, ya no se usa: la duración va por segmento arriba)
#
# Voz en off + subtítulos de los reels generados (tools generate_audio +
# explainer_video del MCP). Encendido por defecto: cada reel text-to-video de 2+
# segmentos sale narrado (una línea del LLM por shot) con subtítulos quemados.
# REEL_VOICEOVER=1                            # 0|false apaga la voz (stitching mudo, como antes)
# HIGGSFIELD_MCP_TTS_MODEL=seed_audio         # default de voz si el form/sheet no elige modelo:
#                                             # seed_audio (ByteDance, ~0.0067 cr/carácter) |
#                                             # elevenlabs (~0.003 cr/carácter — se traduce a
#                                             # text2speech_v2 + variant y EXIGE voz: sin VOICE_ID
#                                             # se usa el preset "Elena" de list_voices)
# HIGGSFIELD_TTS_VOICE_TYPE=                  # preset|element — vacío = voz por defecto del server
# HIGGSFIELD_TTS_VOICE_ID=                    # UUID de list_voices (python mcp_bootstrap.py --voices)
# HIGGSFIELD_SUBTITLE_FONT=patrick            # patrick|caveat|marker|anton — none apaga los subtítulos
#                                             # (0.05 créditos por bloque con voz; el ensamblaje es gratis)
# HIGGSFIELD_OAUTH_REDIRECT=                  # override del redirect URI del flujo OAuth web
#                                             # (default: {origen del frontend}/api/connections/higgsfield/callback)

# ── Cloud API de Higgsfield (LEGACY / retirado) ───────────────────────────────
# Ya NO se usa para generar (el backend activo es el MCP de arriba). Se conservan
# las variables por si hay que hacer rollback al cliente viejo (key+secret, pool
# de créditos propio de cloud.higgsfield.ai). Dejalas vacías salvo rollback.
# HIGGSFIELD_API_KEY=
# HIGGSFIELD_API_SECRET=
# HIGGSFIELD_MODEL=higgsfield-ai/soul/standard
# HIGGSFIELD_RESOLUTION=1080p
# HIGGSFIELD_VIDEO_MODEL=higgsfield-ai/text2video/turbo

# Dashboard de costos — MongoDB (opcional)
# Persiste los eventos de consumo (usage_events) para el dashboard de gastos.
# Si MONGODB_URI está vacío, el tracking se desactiva silenciosamente: la app
# funciona igual, solo que no registra costos (cero latencia, no rompe el pipeline).
# Local: mongodb://localhost:27017  ·  Atlas: mongodb+srv://usuario:pass@cluster.../
MONGODB_URI=
# Base de datos a usar (default: qbyk_aima)
# MONGODB_DB=qbyk_aima
